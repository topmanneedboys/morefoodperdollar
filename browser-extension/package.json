{
  "name": "valuepilot-browser-extension",
  "version": "101.0.0",
  "private": true,
  "scripts": {
    "build:model": "node tools/train-local-ai.mjs",
    "check:model": "node tools/train-local-ai.mjs --check",
    "package": "node tools/package.mjs",
    "lint:firefox": "npm run package && web-ext lint --source-dir dist/firefox --warnings-as-errors",
    "check": "npm run check:model && npm test && npm run lint:firefox",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "web-ext": "^10.6.0"
  }
}
